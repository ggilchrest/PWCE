{
  "name": "pwce",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "validate": "node src/contract-foundation/validate-fixtures.js",
    "validate:compatibility": "node scripts/validate-pwce-compatibility-lock.mjs",
    "generate:gateway-bundle": "node scripts/generate-gateway-bundle.mjs",
    "dev": "node src/http/dev-server.js",
    "studio:generate-recovery-codes": "node scripts/generate-studio-recovery-codes.mjs"
  },
  "devDependencies": {
    "ajv": "8.18.0",
    "ajv-formats": "3.0.1"
  }
}
