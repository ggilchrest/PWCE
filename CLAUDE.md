# PWCE

Read `AGENTS.md` in this directory and follow it — it defines the repo layout (spec-only today; specs live in the separate `.private` git repo) and the user-documentation requirement governed by `.private/standards/DOCUMENTATION.md`, including the required release note for every user-visible change.
