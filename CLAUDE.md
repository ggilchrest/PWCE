# PWCE

Read `AGENTS.md` in this directory and follow it. The root contains the implementation and public artifacts; specifications live in the separate `.private` Git repository. The file also defines the user-documentation requirement governed by `.private/standards/DOCUMENTATION.md`, including the required release note for every user-visible change.
